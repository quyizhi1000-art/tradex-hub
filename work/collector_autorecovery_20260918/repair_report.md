# 2026-09-18 自动补采接续修复

## 已完成

修复已由受管启动器加载：Collector PID 55724，Dashboard PID 58088。17:13:36 回读确认心跳约 1.1 秒，接口已包含新的 next_retry_at，网页实际返回的 app.js 与磁盘逐字节一致。

今天仍为 238/239 个真实有效分钟。重启前后全部 239 条分钟记录的状态、真实快照 ID、版本摘要、错误码、重试时间逐项相同。唯一缺口仍为 13:00 HistoricalTrajectoryNotPublished，未生成替代数据、未重新排队。run_id 仍为 93，没有因重启而重跑已完成分钟。

## 原因和改动

- 原先存在任意 unresolved 分钟，就把整批归为 needs_attention；自动续跑只识别旧 automatic 批次的 retrying。今天自动批次 90 遇到终止性缺口后，其他可补缺口没有继续进入自动批次；普通单分钟后台尝试仍可能发生，不能说整个进程停止。
- collection_store.py 现在优先识别仍待处理/重试的分钟，按最新批次接续（包括手动发起后中断的批次），兼容已落盘的旧 needs_attention 状态。
- 批次准备/收尾失败仍保留 failed 和错误详情，但同日会按持久化完成时间加 60 秒自动续跑。读取和重启不会推迟这个时间；不承诺跨日自动获取历史数据。
- collector.py 在自动补采时保留每分钟较晚的重试时间，防止重开批次冲掉退避间隔；手动立即重试行为保留。
- collection_contracts.py 暴露可空 next_retry_at，页面 app.js 显示自动重试计划。已经真实修复的分钟不再投影旧的活动错误，但原始失败审计保留。
- collector_worker.py 对未收到停机信号的意外循环返回重新打开持久化所有者；正常停止仍立即退出。

没有增加新进程、调度器、数据源、数据库字段或放宽数据验收。复用此前已修好的批次 checkpoint 和精确分钟校验。

## 故障与回归验证

- 三个新的账本场景先在旧实现失败：准备失败后自动接续、旧 needs_attention 接续、修复后清除活动错误。运行守护的意外返回测试也先复现失败。
- 采集账本与守护第一轮 63 passed；随后加入退避保护，账本、Dashboard API、架构边界 73 passed；扩展自动/手动中断接续和页面文案兼容后，相关 3 passed。共覆盖 102 个不同的 Python 用例。
- Node 页面恢复状态测试 5 passed。
- 故障链使用隔离 SQLite、真实 Collector/Store/History、可控时间和模拟来源：准备超时 → 冷却 → 重开数据库 → 一个终止缺口、一个临时超时、一个成功 → 自动再次补齐临时缺口。无人工排队，终止分钟只尝试一次，成功分钟不再采集。
- 验证既有拒绝过期/错误分钟、真实历史指针对账、只读接口不采集、人工重试、受控停机行为。
- 最终改动对照任务开始备份检查；保留工作区其他任务改动。app.js 备份差异中额外出现的 pending_candidates 变更不是本任务编辑，未撤销。Windows CRLF 识别下，本任务涉及文件 git diff --check 通过；未为换行格式全量改写文件。

## 验收边界

未跑全仓库测试、CI 或浏览器交互；页面变化由 Node 状态测试和实际服务字节比对验证。下一交易日自然运行尚未发生，不能据此承诺未来所有来源不再超时。13:00 的来源历史缺口仍如实保留。

证据：status_before.json、slots_before.json、status_after.json、runtime_verification.json、source.diff。此前实际补齐 44 分钟及精确源数据核对见 ../recovery_resume_20260918/repair_report.md。
