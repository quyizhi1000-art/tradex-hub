# 双拐付费优先、手动重扫与窗口轮询修复

2026-09-18，修改已加载到受管 Collector 和网页看板。

## 数据来源与真实重扫

历史指标和当日 ST 名单继续使用原付费源。报价改为一次付费全市场请求优先，仅对缺失、过期或价格字段不合格的股票使用备用批量报价；原有指标计算、复权保护和去重规则保留。新增逐股来源及扫描来源计数，保留原始报价时间。

11:47:27 手动重扫，11:47:32 完成。当前为午休，使用真实午盘收市行情；没有伪造当前成交或补算遗漏时刻。

| 项目 | 实际结果 |
|---|---:|
| 目标股票 / 取得有效报价 | 3,043 / 3,043 |
| 付费 Tushare 提供 | 3,034 |
| 腾讯备用补充 | 9 |
| 通过指标核验 | 2,921 |
| 复权基准变化排除 | 115 |
| 历史缺失排除 | 7 |
| 命中 | 72 |

结果为 partial，因为 122 只未通过指标核验；这与报价完整覆盖是不同指标。新记录标记“手动重扫／午盘收市行情”，独立保存。原有 11 份扫描档案哈希完全不变。最新接口、历史接口和实际窗口均显示该记录及 72 只命中。

执行手动写入前停止 Collector，完成后恢复，未新增调度器或常驻进程；下一计划扫描为 13:00。Collector PID 70940，Dashboard PID 50320。

## 窗口问题及浏览器验证

问题来自每 30 秒自动读取历史时先清空再重建页签和结果，即使内容不变也会使滚动及指标展开状态丢失。

已改为后台保留现有内容，响应相同不重建；新扫描到来时保留当前选中的旧扫描。只在该扫描数据、相关行业信息或用户选择实际改变时更新结果。网络失败保留已有证据。

使用实际桌面页面和真实 72 只记录，调用页面原本注册的轮询函数，对比本次修改前脚本与修复后脚本：

- 修复前：表格被替换，指标折叠，实际滚动容器从 400 回到 0。
- 修复后：表格和指标节点保持相同，指标保持展开，滚动位置仍为 400，删除节点数为 0。
- 实际行业筛选“白色家电 3”在轮询后保留；页面无脚本错误。
- 浏览器验证脚本初次非零滚动断言定位到了不滚动的 dialog；改为实际滚动的内部容器后上述断言通过。

## 验证及变更范围

- 60 项 Python 检查通过：付费优先、仅补缺、过期/无时间/无效字段、主源失败、来源归属、午休门槛、强制扫描时间边界、不可变档案、原有筛选保护及架构边界。
- 1 项受影响页面契约检查通过；9 项 JavaScript 行为检查及语法检查通过。
- 真实付费重扫、运行中 API 回读、服务脚本与源文件字节一致检查、旧档案哈希核验、桌面浏览器前后对比通过。
- 最终按任务开始时备份审阅差异，无无关删改；未运行全仓测试、CI、移动端或下午自然定时扫描。没有改动账号权限、密钥、其他能力的数据源优先级。
- 代码范围：报价网关、付费路由注册、双拐扫描服务、页面轮询；对应测试及数据契约文档。新测试文件为 test_intraday_paid_quotes.py。

证据：forced-result.json、acceptance.json、window-verification.json、stable-scan-window.png、before-scan-hashes.json；既有文件的本次差异见 changes.diff。
