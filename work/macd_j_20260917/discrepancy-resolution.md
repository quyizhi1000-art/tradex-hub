# 2026-09-17 MACD 与 J 线收盘结果核对

修复前：盘中模块 19:56 的收盘价推算为 43 只；选股中心 20:36 的正式指标存档为 56 只。43 只是 56 只的子集。

逐只重放原始筛选输入，少的 13 只原因为：

- 盘中金叉容差 `DIF - DEA > 0.004`：000725.SZ、000809.SZ、000810.SZ、002127.SZ、002156.SZ、002350.SZ、002453.SZ、600418.SH、600966.SH、601163.SH、603726.SH。
- 盘中复权变化保护：002283.SZ、601018.SH。

正式策略使用数据源已发布的日线指标，金叉判断为昨日 DIF ≤ DEA 且今日 DIF > DEA。原手动收盘扫描只替换了收盘价格，仍走盘中推算和保护过滤，因此收盘后不会自然一致。

## 修正

- 收盘确认从已有选股中心同日 v1 正式存档只读派生，不重新获取行情、不重新筛选、不覆盖正式存档。
- 盘中实时扫描和保护条件保留；没有可用正式存档时保持未确认状态。正式零命中与数据不可用分别处理。
- 最新结果和扫描历史均展示收盘确认，保留原始 43 只推算记录，并补充“预估”说明。
- 正式确认保留原结果 ID、快照修订、指标值、质量及来源信息，不伪造实时报价时间。

本次修改涉及 `stock_selection/store.py`、`stock_selection/intraday_macd_j.py`、`dashboard/watch/app.js`、`dashboard/watch/index.html`，以及盘中 Python/JavaScript 回归测试。工作区其他进行中的改动未回退。

## 验证

- 69 项盘中、MACD/J 策略和架构边界测试通过；1 项相关看板契约测试通过。
- 7 项相关 JavaScript 测试通过；JavaScript 语法检查通过。
- 已加载到受管看板。实际接口两侧均 56 只，代码集合一致，正式结果 ID 一致，选股中心存档内容与修复前相同。
- 实际桌面页面：收盘确认 56 行，选股中心 56 行，逐只代码相同；旧预估 43 行可查看，页面无脚本错误。
- 首轮页面验证遇到共享工作区同时更新造成的渲染函数暂时缺失；后续源码补齐后重新验证通过。本次未修改该渲染函数。
- 未运行全量测试、CI，也未验证下一交易日真实盘中采集；盘中既有规则由针对性回归测试保护。

证据：`discrepancy-before.json`、`discrepancy-verification.json`、`close-parity-ui.json`、`close-confirmed-live.png`。
