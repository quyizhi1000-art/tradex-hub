# 双拐雷达取数修复 — 2026-09-18

修复前：09:45–11:15 每轮请求 3,043 只，只取得 240 只，缺失 2,803 只。根因是股票裸代码 000688、000905 被通用代码助手识别为沪市指数，使两批校验失败；累计两批失败又提前结束扫描。

## 修复

- 在股票行情 mapper 内显式识别深市股票代码；保留独立指数查询规则、金额单位和逐股时间。
- 成功一批后清零连续失败计数，符合既有“连续两批失败停止”约定；保留 80 只批量、45 秒预算、90 秒新鲜度门槛，并记录失败批次原因。
- 未全部核验的扫描标为 partial。实时提示、历史记录和扫描页签明确说明“扫描不完整，不代表全量结果”；已核验个股仍可提示并按日去重。
- 历史档案只在读取时派生状态，原始文件不修改。筛选公式、ST、复权保护、正式收盘确认不变。
- 看板重新启动时发现 HTML 混合换行被 Python 文本读取规范化，导致受管健康检查与磁盘内容不一致。将 HTML 读取改为保留原字节的 UTF-8 解码，健康校验恢复通过。

## 实际取数验证

11:18:19–11:18:39，通过正式行情网关进行只读全量探测，未写入业务扫描存档：

| 项目 | 结果 |
|---|---:|
| 请求 / 取得报价 | 3,043 / 3,043 |
| 未取得报价 | 0 |
| 沪市 / 深市 | 1,624 / 1,419 |
| 通过指标核验 | 2,921 |
| 复权基准变化排除 | 115 |
| 历史缺失排除 | 7 |
| 盘中命中 | 71 |
| 耗时 | 19.984 秒 |

命中数对应该时刻真实行情，并非早盘遗漏时刻的补算，也不代表收盘结果。被保护规则排除的 122 只保持未核验，未伪造指标。

## 运行环境

- Collector 已于 11:19 重启，PID 14464；看板已于 11:20 重启，PID 56636。
- 实际 GET 接口已将早盘扫描标为 partial；HTML 和 app.js 的服务内容与本地文件逐字节一致。
- 已有 8 份当日扫描档案哈希保持不变。
- 下一次正式计划扫描为 11:30；本次交付时尚未执行。页面当前保留 11:15 的原始命中，并标注不完整；71 只是上述独立只读验证的结果。

## 检查与范围

- 针对扫描、行情映射、符号助手、架构边界的 71 项 Python 检查通过；新增测试先复现两项原始缺陷，另有一项测试夹具漏填质量字段，补齐后通过。
- 1 项受影响页面契约检查、2 项 HTML 读取检查通过。
- 8 项 JavaScript 行为检查及语法检查通过，覆盖部分扫描命中/零命中、历史筛选、当日去重、失效撤回与正式确认。
- 最终按修复前备份审阅差异，新增行无尾随空白；Git 检查按 Windows CRLF 配置通过。未修改无关工作区改动。
- 未运行全仓测试、CI 或真实浏览器操作；显示逻辑由行为测试和实际服务文件回读核验。未新增进程类型、调度器、数据源或缓存。

范围：行情 mapper、盘中报价网关、双拐扫描服务、页面 app.js、HTML 读取函数、相关测试与数据契约文档。完整差异见 repair.diff。

证据：repair-live-probe.json、repair-quotes.json、runtime-loaded.json、original-scan-hashes.json。
