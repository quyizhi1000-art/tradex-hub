# 恢复原条件并保留盘中新增展示

2026-09-18：恢复 `macd-j-upturn-main-board.v1` 为盘后注册策略、盘中计算与收盘确认规则。仍要求昨日 DIF ≤ DEA、当前 DIF > DEA，以及 J 当前上升、当天或此前 1～3 日拐头。盘中舍入保护、数据质量检查、付费优先与补充报价路径均未改变。

每轮展示当前符合原条件的股票减上一交易日 v1 存档。开盘第一轮符合的也算新增；不减当天上一轮结果。历史 v2 记录保留并标注已停用，混合版本的历史读取按日期和版本分别加载基准。

## 真实结果

- 原版 2026-09-17 存档：56 只，结果 ID `stock-selection-strategy-result:27d4b0b79d2f3cfd417ae601`。
- 2026-09-18 13:35:59 手动重扫：74 只命中，与昨日存档无交集，显示 74 只新增。逐只验证昨日 DIF ≤ DEA，已排除“昨天已金叉、今天仅 J 再次回升”的放宽情形。
- 本轮请求 3043、核验 2913；报价来自 Tushare 3004、腾讯补充 33。130 只未通过完整校验，页面维持扫描不完整状态，不声称全量覆盖。
- 所有已有策略档案摘要和原始扫描文件摘要保持不变。盘后页面、扫描接口、扫描窗口逐只名单及实际提供的页面文件均已核对。
- 真实桌面页面轮询后表格节点不变、指标详情仍展开、滚动位置仍为 400，无页面异常。

## 检查与运行

- 108 项相关 Python 检查通过，包括开盘第一轮纳入、重复扫描保持昨日基准、版本隔离、数据校验、存档兼容、看板契约及架构边界。新增开盘测试最初因测试中的下次扫描时间早于本轮而失败，修正为本轮后 15 分钟后通过；生产时效规则未改。
- 11 项 JavaScript 行为检查和语法检查通过，最终范围内差异检查通过。未运行全仓套件；下一交易日自然运行未实测。
- 分析器初始化约 88 秒，超过原管理入口的 60 秒等待。临时复制修改管理脚本的操作被自动审批拒绝，未执行。改用原分析器入口启动，核验进程身份、启动时间和真实运行心跳后，通过原管理函数登记。没有修改管理脚本或业务初始化流程。
- 验收时管理进程：Analysis 35836（工作子进程 68680）、Collector 45984、Dashboard 44864。
- 主要修改：`macd_j.py`、`strategies.py`、`intraday_macd_j.py` 恢复原规则；`intraday_increment.py` 按版本使用昨日基准；页面规则标签与停用记录说明同步。规范见 `G:/money/docs/intraday-macd-j.md`。

明细：同目录 `scan-receipt.json`、`publish-receipt.json`、`live-verification.json`、`changes.diff`。
