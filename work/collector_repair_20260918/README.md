# 2026-09-18 盘中轨迹断线复发修复

## 原因

11:24 实际 API 显示目录 1000 条、热点 320 条，999 条存在分钟缺口。Collector 正常运行，但原始全量板块快照截至当时只落下 30 个分钟；日志同时存在行情源连接失败和其他组件字段校验失败。不能把采集进程在线等同于数据完整。

昨天实现的盘中策略仍限制为每轮一个未分类热点，普通板块只有收盘后入队，午休也未切换为批量。这不足以消化今天的规模。另一个确定缺陷是：gateway 在盘中非 22–46 秒窗口返回 None（没有网络请求），却被目录调度器记为源端无进展失败，产生退避；调用还使用了目录计算前的旧时间，而不是请求时的时间。

昨天收盘恢复通过不能证明盘中连续运行通过。上一轮验收范围不足，本次保留该事实。

## 本次代码修改

- `tradex/src/tradex/dashboard/sector_catalog_collector.py`：所有当日发现、支持同源分钟的缺口板块均可入队。优先处理超过五分钟的旧缺口，再优先默认展示的十二个活跃热点、其他热点、普通板块。盘中最多四个目标、十秒准入预算；午休及收盘最多三十二个目标、二十秒预算；每次请求前用实际时钟检查窗口，跨入交易时段后不会继续使用午休预算无条件发请求。
- `tradex/src/tradex/data_gateway/sector_flow.py`：公开原有时间窗判断，复用原 gateway、缓存、限流及超时，没有增加直连 provider。
- `tradex/src/tradex/market_watch/sector_catalog.py`：未准入记 deferred 而非源端失败；自动解除可以由原尝试时间严格证明是非窗口请求所造成的旧退避，保留尝试审计计数。真实窗口内失败保留原退避。
- `tradex/src/tradex/dashboard/collector_worker.py`：请求使用实际时钟；午休/收盘仍有积压时轮间等待改为一秒，正常交易时保留原循环间隔。
- `tradex/docs/sector-catalog.md`：同步上述政策和运行边界。

没有运行一次性批量回填脚本，没有改图表断线规则，没有插值或复制旧值。补采由加载新代码后的正式 Collector 自动完成。没有增加进程、Agent、外部调度器或新的必采板块目标。

## 验证及执行记录

50 个不同的相关 Python 用例通过：目录 22、Collector worker 26、架构边界 1、gateway optional 准入保护 1。覆盖普通板块盘中准入、午休批量、请求时窗口边界、非窗口不创建失败、旧误退避与真实失败区分，以及原有重启/发布故障恢复。首次较完整的 worker 测试发现旧 FakeCollector 缺少昨日新增的 start()，导致监督器在测试中反复重试；终止本任务的测试进程、补齐测试替身初始化后，该域测试通过。

定向 `git diff --check` 通过。全工作区检查报告了其他任务改动的 app.js、styles.css 等文件的换行/空白问题；本任务没有重排或修改这些文件。没有跑全仓测试或浏览器视觉验收。

11:30 重启原受管 Collector，新 PID 38944。`before.json` 是加载前目录；`automatic_progress.jsonl` 是只读验收监控。恢复程序逐轮发现、下载、落盘并发布，默认十二条热点在 11:33 前已全部覆盖上午 120 分钟，后续检查持续完整。正式数据没有人为删点制造故障。

本轮源端原始快照稀疏和其他组件字段错误尚未全部根治；这次修复的是已证实的目录补采调度缺陷。下午自然交易时段尚未到达，不能提前宣称下午持续采样已验证。代码尚未提交或推送。

补充验证：在自动补采中途加载失败计数修正并重启，最终受管 PID 47360。重启前已完整的 555 条曲线在重启后全部保持完整且 points_revision 不变，队列从剩余目标继续推进，见 `restart_verification.json`。另修正 failed_targets 仅统计仍存在缺口的失败目标；由共享 gateway 的其他入口补齐时，不再把保留的历史失败审计计入待处理失败。

11:57:30 回读整体盘面账本为 81 个有效、40 个待重试、118 个尚未采集时点，采集器 running 且心跳新鲜。这与全量板块资金曲线是不同的覆盖集合，不能用曲线完成状态代替整体盘面完成状态，见 `collector_status.json`。

## 上午最终验收

12:01:17 只读监控确认正式 Collector 自动恢复完成：1000/1000 条曲线、其中 322 条热点，全部覆盖截至 11:30 的 120 个上午分钟。恢复状态 complete，missing_series=0，failed_targets=0。目录版本为 `f7c9de1b244f5f73ff749faa34649285cde99d903ce57012625c153d997d84c3`。

`catalog_verification.json` 验证了全部 1000 条线上版本绑定曲线：逐点与真实原始观测或规范历史记录核对，检查时间、值、摘要和发布版本，错误数 0。`catalog_render_verification.json` 使用实际服务的页面脚本，验证其与工作区文件摘要一致；1000 条曲线各保留 120 点、形成一段连续上午路径，断线数 0。这是 API 与实际绘图函数验证，未执行浏览器视觉验收。

报价新鲜度仍独立保留为 quote_as_of=11:26，missing_latest=8；曲线历史补齐没有伪造报价时间，也不代表最新报价或整体盘面账本全部完整。下午自然运行尚未验证。
