interface:
  display_name: "需求方案复核"
  short_description: "仅在显式调用时拆解需求、复核方案并在执行前请求确认"
  default_prompt: "Use $requirement-plan-audit to audit this requirement and return a final implementation plan for my approval."

policy:
  allow_implicit_invocation: false
